# FF14 物价百科 Web v1.1.1

纯静态网页版本，支持中文搜索、国服价格查询和国服 Wiki 词条直达。

## 主要功能

- 纯静态部署，无需自建后端
- 本地双语映射表搜索
- 国服 Universalis 价格查询
- `全部 / HQ / 非 HQ` 价格查看
- 按国服大区筛选市场价格
- 当前区服销售排行
- 配方成本与利润计算
- NPC 商店售价与精确坐标展示
- 国服 Wiki 词条直达
- 国服实时招募查询与详情
- 主题颜色调节

## 最新更新

2026-08-23

- 新增“查招募”入口，实时读取国服招募列表，支持中文搜索、大区与分类筛选、分页、刷新和详情查看
- 招募详情补齐目的、条件、战利品规则、类型、职业槽位和剩余时间等中文显示
- 修正特殊迷宫分类参数为 `V&C Dungeon Finder`，并兼容 `FieldOperations` / `AdventuringForays` 等返回别名
- 分页请求按批次并发读取并按招募 ID 去重；部分页面失败时会明确提示，不会把残缺数据显示为完整结果
- 快速连续切换搜索或筛选条件时会取消旧请求，避免旧结果覆盖当前选择
- 招募第一页返回后立即显示，其余页后台补充；刷新失败保留上次结果，并恢复当前页码、滚动位置和键盘焦点
- 限制异常分页数量，避免错误或恶意响应触发无限后台请求
- 所有远程 JSON 请求都有超时边界；Universalis 区服元数据暂时不可用时，物品与任务搜索仍可继续使用
- 本地预览服务器只允许读取网页运行所需文件，并阻止编码路径穿越、符号链接逃逸和不支持的 HTTP 方法
- 招募数据来自 Remote Party Finder 国服服务；纯网页无法自定义请求 User-Agent，源站策略变化时可能需要同源代理
- 修复物品详情加载时引用失效变量的问题，并清理会覆盖 HQ / 非 HQ 逻辑的重复市场函数
- 招募接口同时兼容 `data` / `listings` 返回字段和空分页；销售排行、自动刷新与快速切换区服均有竞态和失败保留测试
- 图标代理按流限制为 5 MB，Wiki、图标和桌面解析跳转只接受预期的 HTTPS 地址
- 修正“第四期重建用的特供硅砂（检）”与本地映射中的“硅沙”翻译差异，统一解析到 Universalis 物品 ID `31999`
- Wiki 搜索会自动整理括号前后的空格，兼容 `特供硅砂（检）` 与 `特供硅砂 （检）` 等输入
- 本地别名物品优先使用映射与 XIVAPI 信息，不再等待失效的 CafeMaker；市场板加载期间会明确显示“读取中”，避免误判为无价格
- 物品卡片的国服 Wiki 入口改用规范化搜索 URL，避免括号空格差异造成直接文章地址失效
- 对直接打开物品 ID 的场景也优先使用该物品的本地规范名称，避免旧译名“硅沙”污染 Wiki 查询

2026-08-13

- 中文和英文物品联想改为优先读取本地双语映射表，输入 `秘银` 即可快速看到 `秘银矿` 等相关候选
- 模糊搜索和同名条目不再自动跳转，候选列表显示物品或任务 ID，由用户确认后打开详情
- 物品与任务搜索分别容错，CafeMaker 故障时本地物品候选仍可用，远程请求 4 秒超时后静默降级
- 修复连续输入或快速选择时旧请求覆盖新结果的问题，并使用 `?q=关键词` 保留待选择状态
- 市场价格仍由 Universalis 提供；CafeMaker 仅用于远程百科详情和任务数据补充

2026-06-09

- 刷新内置双语物品映射表，补齐 45 个当前可交易但旧映射表缺失的物品
- 补齐 `奥克塞西亚能源包` / `Auxesia Drone Module`，支持中文和英文直接搜索
- 补齐奥克塞西亚、宇宙探索、塔可饼委托、坐骑/宠物/乐谱/装备箱等新增可交易物品
- 修复新增物品图标不显示的问题，旧图标镜像未同步时自动使用 XIVAPI v2 asset 兜底
- 更新前端脚本和映射表缓存版本，减少 GitHub Pages 或浏览器继续加载旧数据的情况

2026-05-29

- 新增当前范围销售排行，支持中国全区、大区、世界服查看价格排行与销量排行
- 新增配方成本与利润计算，点击“计算利润”后再读取材料价格，显示成本总价、当前售卖最低价、税后利润和利润率
- 优化排行加载速度，改用批量并发读取 Universalis 聚合数据，并保留短时预览缓存
- 修复网页端排行物品名和图标显示异常，图标优先走本地代理缓存，外链失败时自动回退

2026-05-25

- 新增国服大区筛选，可直接查看陆行鸟、莫古力、猫小胖、豆豆柴各大区价格
- NPC 商店来源显示售价、NPC 名称和地图位置
- 能确认坐标的 NPC 商店位置精确到 `X:多少 Y:多少`
- 无法确认 NPC 或坐标的商店记录不再作为 NPC 来源卡片展示，避免出现“待确认 NPC”
- 更新静态资源版本参数，强制 GitHub Pages 客户端加载最新脚本

2026-05-22

- 同步桌面端整理后的前端实现到网页端
- 清理重复的搜索结果渲染与启动逻辑，减少旧代码覆盖风险
- 搜索链路保持不变，继续使用双语映射表和 Wiki 兜底
- 修复关联物品点击后详情页偶发回退英文名的问题，映射命中后统一优先显示中文名
- 修复搜索联想结果层遮挡搜索栏的问题，联想面板改为紧贴输入区域展开
- 主题调色改成支持颜色盘和 `RGB` 颜色代码输入
- 保留 `HQ / 非 HQ` 市场排序与统计逻辑

## 本地预览

```powershell
Set-Location 'E:\study\ff14网页'
node tools/serve-static-utf8.js 4173
```

打开：

```text
http://127.0.0.1:4173
```

## 本地验证

```powershell
node --check app.js
node --check party-finder.js
node tools/test-app-regressions.js
node tools/test-static-server.js
node tools/test-party-finder.js
node tools/test-search-ranking.js
node tools/test-market-calculations.js
```

## 部署

推荐直接部署到 GitHub Pages，具体见 [DEPLOY.md](./DEPLOY.md)。

版本记录见 [GitHub Releases](https://github.com/dz-knight/ff14-web/releases)。

## 说明

- 这是纯静态站点，不包含独立后端
- 更详细的本次上传说明见 [RELEASE_NOTES.md](./RELEASE_NOTES.md)
