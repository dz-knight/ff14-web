# FF14 物价百科 Web 更新说明

## v1.0.9 - 2026-08-13

本次更新重点优化中文模糊搜索，并降低 CafeMaker 故障对查询入口的影响。

- 物品搜索优先扫描本地 17,000+ 条双语映射，输入 `秘银` 会立即显示 `秘银矿`、`秘银锭` 等候选
- 中文、英文和同名物品都按相关度排序；同名物品显示 ID，避免误选
- 模糊搜索不再自动跳转，任务名称搜索同样要求用户选择准确结果；直接输入任务 ID 仍可打开任务
- 物品与任务查询独立容错，任一远程查询失败都不会拖垮另一类结果
- CafeMaker 搜索请求增加 4 秒超时；服务不可用时优先保留本地物品候选和 Universalis 市场价格
- 增加搜索与详情加载令牌，避免较慢的旧请求覆盖用户刚输入或刚选择的内容

CafeMaker 当前返回 Cloudflare `530 / 1016`，说明其域名所配置的源站 DNS 无法解析或不可达。市场价格数据源仍是 Universalis；CafeMaker 只承担百科详情和任务等远程数据补充。

## 验证

- `node --check app.js`
- `node --check search-ranking.js`
- `node tools/test-search-ranking.js`
- `node tools/test-market-calculations.js`
- 浏览器验证 `秘银` 候选、同名物品 ID、选择后详情和 Universalis 国服价格

## 2026-06-09 图片修复

新增物品图标不显示的原因是旧图标镜像站尚未同步 7.5x 的部分图标，例如 `023000/023624.png` 会返回 404。

- 前端图标地址优先使用 XIVAPI v2 asset 接口，避开旧镜像未同步导致的 404
- 本地静态调试服务器的 `/__icon` 代理同步增加 XIVAPI v2 兜底
- 更新 `index.html` 中的 `app.js` 缓存版本，确保 GitHub Pages 和浏览器客户端加载修复后的脚本

## 验证

- `node --check app.js`
- `node --check tools/serve-static-utf8.js`
- 本地 `/__icon?path=023000%2F023624.png` 返回 `200 image/png`

## 2026-06-09

本次更新刷新了内置双语物品映射表，补齐 45 个当前可交易但旧映射表缺失的物品。

- 补齐 `奥克塞西亚能源包` / `Auxesia Drone Module`，现在可以直接用中文或英文搜索
- 补齐奥克塞西亚、宇宙探索、塔可饼委托、坐骑/宠物/乐谱/装备箱等相关新增可交易物品
- 映射表继续使用本地国服客户端中文数据 + XIVAPI 英文数据生成，保留中文描述和本地图标路径
- 更新映射表缓存版本，减少 GitHub Pages 或浏览器继续加载旧数据的情况

## 验证

- 重新生成候选映射表并比对旧表，确认本次只补入 45 个缺失可交易物品
- 校验 `奥克塞西亚能源包`、`宇宙金属置物架`、`色素` 等新增条目已存在于网页端映射表
- `node --check app.js`

## 2026-05-29

本次更新重点补齐区服销售排行和配方利润计算，并修复网页端排行图片显示问题。

- 新增“当前区服销售排行”，可按中国全区、大区、世界服查看前 30 名价格排行和销量排行
- 价格排行按当前售卖最低价排序，不再使用“销量 × 价格”作为价格排行依据
- 新增配方成本与利润计算，点击“计算利润”后才读取材料价格，避免拖慢页面初始加载
- 利润计算支持选择中国全区、大区、世界服，并显示材料单价、成本总价、当前售卖最低价、税费、税后利润和利润率
- 材料缺价时会标记“暂无价格”，不输出容易误导的利润结果
- 优化排行加载速度，使用批量并发读取市场聚合数据；短时缓存只用于刷新期间预览或接口失败兜底
- 修复网页端排行物品名显示为 `Item #xxxx` 和物品图标不显示的问题

## 验证

- `node --check app.js`
- `node --check market-calculations.js`
- `node tools/test-market-calculations.js`
- Edge 本地页面验证排行图标通过 `/__icon` 加载，图片 `naturalWidth > 0`

## 2026-05-25

本次更新重点补齐国服大区筛选和 NPC 商店来源信息。

- 新增大区筛选按钮，可按 `陆行鸟`、`莫古力`、`猫小胖`、`豆豆柴` 查看市场价格
- 市场总览和世界服价格表会跟随当前大区筛选刷新
- 普通 NPC 商店来源现在显示 NPC 售价、NPC 名称、地图位置
- 能从数据源确认的 NPC 位置会精确显示到 `X:多少 Y:多少`
- 对缺少明确 NPC 或缺少坐标的数据记录，不再显示“待确认 NPC”，避免误导
- 更新静态资源版本参数，减少 GitHub Pages 客户端继续使用旧脚本的情况

## 验证

- `node --check app.js`
- Chrome headless 烟测橙汁页面，确认沙沙梅洛显示 `黑衣森林 / 格里达尼亚旧街 / X:14.5 Y:8.5`

## 2026-05-20

本次上传主要是网页端前端逻辑和文案同步整理，目标是和桌面端保持一致。

- 同步桌面端已验证的前端实现到网页端
- 修复物品详情中的国服 Wiki 跳转逻辑，物品词条优先直达详细页
- 新增白天 / 黑夜主题切换，并记住用户上次选择
- 修复 `HQ / 非 HQ` 模式下市场总览与世界服价格表的排序和统计表现
- 修复关联物品点击后详情页偶发显示英文名的问题，双语映射命中后统一优先显示中文名
- 修复搜索联想结果层遮挡搜索栏的问题，联想面板位置与间距重新整理
- 修复首页与价格表中的乱码文案
- 清理旧的重复前端覆盖逻辑，减少缓存和运行分叉问题

## 兼容性说明

- 仍然是纯静态网页，无需新增后端
- 页面脚本语法检查通过：`node --check app.js`
